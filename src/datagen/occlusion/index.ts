/**
 * Occlusion Detection Module
 *
 * Provides raycasting-based occlusion detection for 3D scene objects,
 * replacing the previous stub that always returned `visible: 1.0`.
 */

export { OcclusionDetector } from './OcclusionDetector';
export type { OcclusionResult, OcclusionDetectorConfig } from './OcclusionDetector';
