/**
 * Node Base - Re-export from core types for compatibility
 * 
 * Files importing from '../core/node-base' will find NodeDefinition here.
 */

export type { NodeDefinition, NodeBase, NodeInstance, NodeLink } from './types';
