import { Group } from 'three';
export declare class EyeGenerator {
    private seed?;
    constructor(seed?: number | undefined);
    generate(type: 'compound' | 'camera', count: number, size: number): Group;
}
//# sourceMappingURL=EyeGenerator.d.ts.map